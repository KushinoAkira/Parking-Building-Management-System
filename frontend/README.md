
# Parking Building Management — Frontend

React + Vite UI (Figma export). Gốc thiết kế: [Figma — PARKING LOT SWP391](https://www.figma.com/design/rd1r0jWoBuzNS5KOecAqTC/PARKING-LOT-SWP391-UI-UX--Copy-).

## Chạy dev

```bash
pnpm install
cp .env.example .env
pnpm dev
```

Mở http://localhost:5173 — API proxy tới backend tại `/api`.
